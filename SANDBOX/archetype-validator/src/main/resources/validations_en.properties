// This file is intentionally empty to indicate that en is a supported language, even if all English terms are actually in the top-level fallback file validations.properties.
// This is to essentially trick Java into falling back to the root, and not to the JVM's Default locale. If that is e.g. set to de, en would otherwise fallback to de, insteado f the desired root.  